# Dotfiles packages — install with: yay -S --needed - < packages.txt
# Lines starting with # are ignored by yay/pacman

# ── Compositor & Wayland ──────────────────────────────────────────────────────
hyprland
hyprpaper
hyprlock
xdg-desktop-portal-hyprland

# ── Status bar & shell ────────────────────────────────────────────────────────
quickshell          # bar + control center + notifications + Claude sidebar (~/.config/quickshell)

# ── Terminal & multiplexer ────────────────────────────────────────────────────
kitty
tmux

# ── Shell prompt ──────────────────────────────────────────────────────────────
starship

# ── System monitor ───────────────────────────────────────────────────────────
btop

# ── Editor ────────────────────────────────────────────────────────────────────
neovim

# ── Launcher ──────────────────────────────────────────────────────────────────
rofi-wayland

# ── Notifications ─────────────────────────────────────────────────────────────
libnotify

# ── File manager & deps ───────────────────────────────────────────────────────
yazi
fzf
fd
ripgrep
zoxide              # yazi navigation integration
ffmpegthumbnailer   # yazi: video thumbnails
unar                # yazi: archive preview
imagemagick         # yazi: image conversion
poppler             # yazi: PDF preview

# ── Wallpaper daemon ──────────────────────────────────────────────────────────
awww                # wallpaper script uses awww/awww-daemon (not swww)

# ── Color theme generation ────────────────────────────────────────────────────
matugen

# ── GTK & theming ────────────────────────────────────────────────────────────
gtk3
gtk4
gsettings-desktop-schemas
adwaita-icon-theme

# ── Audio ─────────────────────────────────────────────────────────────────────
pipewire
pipewire-pulse
wireplumber
pavucontrol
playerctl           # media-key bindings (XF86Audio*)

# ── Brightness ────────────────────────────────────────────────────────────────
brightnessctl

# ── Network ───────────────────────────────────────────────────────────────────
networkmanager
network-manager-applet  # nm-connection-editor: GUI fallback (control center handles wifi/bt)

# ── Screenshots & clipboard ───────────────────────────────────────────────────
grim                # keybind: screenshot
slurp               # keybind: area selection for screenshot
wl-clipboard        # wl-copy/wl-paste: clipboard + cliphist integration
cliphist            # keybind: clipboard history via rofi

# ── Fonts ─────────────────────────────────────────────────────────────────────
ttf-cascadia-code-nerd  # kitty, quickshell, rofi
ttf-jetbrains-mono-nerd # hyprlock

# ── Session & polkit ─────────────────────────────────────────────────────────
wlogout             # quickshell power button (bar + control center)
polkit-kde-agent    # autostart: /usr/lib/polkit-kde-authentication-agent-1

# ── Media ─────────────────────────────────────────────────────────────────────
mpv                 # subtui audio backend

# ── Audio visualizer ─────────────────────────────────────────────────────────
cava

# ── Build & scripting ─────────────────────────────────────────────────────────
git
base-devel
python
python-gobject      # keybinds-viewer.py (GTK4 app)
